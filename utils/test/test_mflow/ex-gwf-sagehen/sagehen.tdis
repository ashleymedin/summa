# Time discretisation - SECONDS, one stress period of 744 hourly steps.
# The coupler advances MODFLOW exactly once per SUMMA forcing data step, so:
#   * the MODFLOW time step length (here perlen/nstp) must equal SUMMA's data_step
#     - the forcing file's data_step attribute, 3600 s for this Sagehen forcing;
#   * the total number of MODFLOW steps (744) must be >= SUMMA's numtim (number of
#     data steps in the simulated period).
#   72 steps * 3600 s = 259200 s  (2019-08-01 00:00 .. 2019-08-03 23:00, hourly)
#   short shallow-water-table coupling test; bump to 744 / 2678400.0 for the full month
BEGIN OPTIONS
  TIME_UNITS  SECONDS
END OPTIONS

BEGIN DIMENSIONS
  NPER  1
END DIMENSIONS

BEGIN PERIODDATA
#  perlen      nstp   tsmult
   259200.0    72     1.0
END PERIODDATA
